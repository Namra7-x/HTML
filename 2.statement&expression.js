//expression => code that produces a value example = 4 + 3 ans = 7

//statement => code that performs action but doesn't return a value example => let name = "namra";

//perform in console in expression it gives 7 while statement gives undefined bcz action performed.